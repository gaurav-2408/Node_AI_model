{
  "name": "node_ai_model",
  "version": "1.0.0",
  "description": "DynamoDB AI Query Interface",
  "main": "index.js",
  "scripts": {
    "start:backend": "cd backend && node index.js",
    "start:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && react-scripts start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
